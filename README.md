# socket-server

Realtime Socket.IO server for lolboost.gg.

Required Hostinger environment variable:

```text
REALTIME_SECRET=put-a-long-random-secret-here
```

Optional:

```text
ALLOWED_ORIGINS=https://lolboost.gg,https://www.lolboost.gg
```

Start command:

```bash
npm start
```
